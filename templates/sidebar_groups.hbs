<ul class="nav nav-list">
  <li class="nav-header">» groups «</li>
    <li>
      <a href="/find/groups" data-route class="btn btn-success btn-sidebar">Find your voice</a>
    </li>
  {{#unless groups}}
    <i>No groups :(</i>
  {{/unless}}
  {{#each groups}}
    <li>
      <a href="/groups/{{_id}}" data-route>
        {{#if unread}}
          <span class="badge badge-important">{{unread}}</span>
        {{/if}}
        {{name}}
      </a>
    </li>
  {{/each}}
</ul>